import React from "react";
import ContactForm from "./ContactForm";

const Contacts = () => {
  return (
    <section>
      <h2>Контакты</h2>
      <ContactForm />
    </section>
  );
};

export default Contacts;
